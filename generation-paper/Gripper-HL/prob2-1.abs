===========================
Subtypes:

Subtypes of gripper:
	st1(gripper) = { gripper1 gripper2 }

Subtypes of ball:
	st2(ball) = { ball1 ball2 ball3 ball4 }
	st3(ball) = { ball5 ball6 ball7 ball8 }

===========================
Feature Mapping:

N0 = ( { st1(gripper) st2(ball) } , { carry(ball, gripper) he(gripper) } )

N1 = ( { st1(gripper) st2(ball) } , { carry(ball, gripper) le(gripper) } )

N2 = ( { st1(gripper) st3(ball) } , { carry(ball, gripper) he(gripper) } )

N3 = ( { st1(gripper) st3(ball) } , { carry(ball, gripper) le(gripper) } )

N4 = ( { st1(gripper) } , { free(gripper) he(gripper) } )

N5 = ( { st1(gripper) } , { free(gripper) le(gripper) } )

N6 = ( { st2(ball) } , { at(ball, room1) } )

N7 = ( { st2(ball) } , { at(ball, room2) } )

N8 = ( { st3(ball) } , { at(ball, room1) } )

N9 = ( { st3(ball) } , { at(ball, room2) } )

B0 = at-robby(room1)

B1 = at-robby(room2)

